// Conditionals: If Statements

// Decision tree!
if (playerOne === computer) {
  //tie game
} else if (playerOne === "rock") {
  if (computer === "paper") {
    //Computer wins
  } else {
    // playerOne wins
  }
}
